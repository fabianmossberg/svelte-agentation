# Porting ledger

One row per upstream source file. This is the authoritative record of what
has been ported, how, and where we deviate. Update it in the same PR as the
code change. Upstream paths are relative to `upstream/package/src/`; ours to
`src/lib/`.

**Modes**

- `verbatim` — copied as-is; must stay diff-minimal against upstream.
- `adapted` — copied with documented divergences (each one has a
  `// DIVERGENCE(upstream):` comment at the site and a note here).
- `rewritten` — reimplemented in Svelte; upstream file is the behavioral spec.
- `skipped` — deliberately not ported (say why).
- `deferred` — planned for a later phase (link the phase).

## Utils & types

| Upstream | Ours | Mode | Status | Divergences |
|---|---|---|---|---|
| `types.ts` | `types.ts` | adapted | done (#6) | (1) absorbed `OutputDetailLevel` + `ReactComponentMode` from `components/page-toolbar-css/index.tsx` (upstream leaks them; see RESEARCH.md) — each marked `// DIVERGENCE(upstream):` at the site. (2) **Whole-file formatting:** unlike the diff-clean adapted ports (`generate-output.ts`, `utils/index.ts`, which are Prettier-excluded to stay byte-identical), `types.ts` is **Prettier-managed** and reformatted to this repo's style (tabs, single quotes), so it is **not** byte-identical to upstream. The type *shapes* are upstream's verbatim — schema compat is unaffected (compat fixtures stay byte-identical) — but upstream type changes are re-applied by hand rather than as a clean diff. Accepted for this more-heavily-adapted file; marked with a file-level `// DIVERGENCE(upstream):` note. Decided in the #15 gate (criterion 4 relaxed accordingly). |
| `utils/element-identification.ts` | `utils/element-identification.ts` | verbatim | done (#7) | byte-identical (`diff` empty); excluded from Prettier in `.prettierignore` to preserve upstream's 2-space/double-quote style — ESLint still lints it. New jsdom unit tests added (upstream ships none). |
| `utils/storage.ts` | `utils/storage.ts` | verbatim | done (#8) | byte-identical (`diff` empty); excluded from Prettier in `.prettierignore`. First verbatim port with an import — its extensionless `import type { Annotation } from "../types"` forced relaxing the scaffold's `NodeNext` moduleResolution to SvelteKit's default `bundler` in `tsconfig.json` (published artifact unchanged — type-only import is erased). New jsdom unit tests added (upstream ships none). |
| `utils/freeze-animations.ts` | `utils/freeze-animations.ts` | verbatim | done (#9) | byte-identical (`diff` empty); excluded from Prettier in `.prettierignore`. Zero imports; patches `setTimeout`/`setInterval`/`requestAnimationFrame` as an import side effect, state parked on `window.__agentation_freeze` to survive HMR (kept verbatim per issue #9 out-of-scope). Its `ReturnType<typeof setTimeout>` annotations clash with the `NodeJS.Timeout` overload that `@types/node` (pulled in by vitest) appends to the global `setTimeout`; accommodated **outside** the port by `src/ambient-dom-timers.d.ts`, which appends a trailing DOM `number` overload so `ReturnType` resolves to `number` (upstream's browser assumption) — see that file's header. New jsdom unit tests added (upstream ships none). |
| `utils/screenshot.ts` | `utils/screenshot.ts` | verbatim | done (#10) | byte-identical (`diff` empty); excluded from Prettier in `.prettierignore`. Its guarded dynamic `import("modern-screenshot")` (the DOM-to-canvas dep) is left **undeclared and uninstalled to mirror upstream** — which declares it nowhere — so absent consumers get the graceful stroke-only fallback, not an install error (declaring it even as an optional peer dep would make pnpm's `auto-install-peers` pull it into our tree, defeating the point). The uninstalled import would otherwise break the toolchain, accommodated **outside** the port: `src/modern-screenshot.d.ts` (minimal `domToCanvas` ambient `declare module` shim) keeps `pnpm check`/`pnpm build` from failing TS2307, and a `vite.config.ts` plugin marks the specifier external so the dev server doesn't abort on the unresolvable dynamic import (under vitest a `test.alias` → `src/test/modern-screenshot-stub.ts` makes it resolvable so `vi.mock` can drive the present/absent probe paths). Same accommodate-outside-the-port approach as `src/ambient-dom-timers.d.ts`. New jsdom unit tests added (upstream ships none). |
| `utils/sync.ts` | `utils/sync.ts` | verbatim | done (#11) | byte-identical (`diff` empty); excluded from Prettier in `.prettierignore`. Pure-`fetch` client for the server protocol; the `../types` relative import resolves unchanged. No divergences. New vitest unit tests added (upstream ships none) with a mocked global `fetch` asserting, per exported function, the exact URL/method/headers/JSON body, the parsed-JSON return, the status-bearing throw on a non-ok response, and that a network-error rejection propagates (the local-only fallback contract is the caller's, Phase 4). |
| `utils/generate-output.ts` | `utils/generate-output.ts` | adapted | done (#12) | sole divergence is the import block, marked `// DIVERGENCE(upstream):` — the two enums (`OutputDetailLevel`, `ReactComponentMode`) plus `Annotation` are imported from `../types` instead of upstream's `../components/page-toolbar-css` (upstream leaks the enums from the toolbar component; see RESEARCH.md). Everything past the imports is byte-identical to upstream (`diff <(tail -n +6 upstream/…) <(tail -n +3 src/lib/…)` empty), so the file is Prettier-excluded in `.prettierignore` like the verbatim ports; `import` became `import type` to satisfy the scaffold's `verbatimModuleSyntax` (all three names are type-only — erased from the artifact). React fields (`sourceFile`/`reactComponents`) keep their output logic, unpopulated until Phase 7. New vitest tests added (upstream ships none): a jsdom file for the window-present paths (all 4 detail levels exact-string, compact 30-char truncation, `nearbyText`-suppressed-by-`selectedText`, `sourceFile`/`reactComponents` presence/absence) and a `node`-environment file for the deterministic window-absent paths (viewport `unknown`, forensic Environment block without URL/UA/Timestamp/DPR) — matching how the Phase 1 compat fixtures are generated. |
| `utils/react-detection.ts` | — | skipped | n/a | React-fiber-bound; Svelte equivalent is Phase 7 |
| `utils/source-location.ts` | — | skipped | n/a | React-fiber-bound (`_debugSource`); Svelte equivalent is Phase 7 |
| `utils/index.ts` | `utils/index.ts` | adapted | done (#13) | sole divergence is the dropped `source-location` re-export, replaced in place by a `// DIVERGENCE(upstream):` line (React-fiber-bound; Phase 7) — `element-identification`/`storage`/`sync` re-exported in upstream's order, no extra re-exports added (upstream's barrel also omits `freeze-animations`/`screenshot`/`generate-output`). Kept upstream's double-quote/extensionless style and Prettier-excluded so upstream changes to the barrel apply as a clean diff. New vitest smoke test imports `identifyElement`/`loadAnnotations`/`createSession` through the barrel and asserts each is a function. |

## Components

| Upstream | Ours | Mode | Status | Divergences |
|---|---|---|---|---|
| `components/page-toolbar-css/index.tsx` (4.7k LOC) | `components/page-toolbar/` + `internal/*.svelte.ts` | rewritten | in progress | state decomposed into runes controllers (annotations / picker / markers / settings); behavior must match. **`internal/settings.svelte.ts`** done (#23) — the settings + theme model and its persistence, plus the model constants the panel imports (upstream declares all of this inline in the monolith: model L142–227, settings load L551–563, theme load L695–704, settings save L720–728, theme save L730–738). Exports `ToolbarSettings` (importing `MarkerClickBehavior` from the marker barrel — never redefined), `DEFAULT_SETTINGS`, `COLOR_OPTIONS`, `injectAgentationColorTokens` (called once at module load, SSR-guarded, mirroring upstream's L227 top-level side effect; injects `<style id="agentation-color-tokens">` setting `--agentation-color-accent` per `[data-agentation-accent]`), and re-exports `OutputDetailLevel`/`ReactComponentMode` from `types.ts` (don't redefine — they were moved there in Phase 1). `SettingsController` holds `settings`/`isDarkMode` `$state` behind getters (+ a `theme` getter for the `data-agentation-theme` value); `load()` hydrates both from the upstream localStorage keys (`feedback-toolbar-settings`/`feedback-toolbar-theme`) with upstream's validation (unknown `annotationColorId` → default, malformed/missing settings → `DEFAULT_SETTINGS`, missing theme → dark); `patch(partial)` + `toggleTheme()` mutate and persist. **Persistence is imperative** (`#persistSettings`/`#persistTheme` called from each mutation, not a `$effect`) — same precedent as the annotations/markers controllers; `load()` never persists, so it can't clobber storage on mount. New vitest tests (`settings.svelte.test.ts`, 15) cover the model constants, the injection side effect + idempotency, defaults, the full load matrix (empty/partial-merge/known+unknown color/malformed/theme), patch persistence round-trip through a fresh controller, successive-patch merge, and `toggleTheme` persistence. **`internal/picker.svelte.ts`** done (#17) — feedback-mode activate/deactivate, document-level hover tracking, shadow-DOM-aware hit-testing, and click-to-create-pending. Maps these monolith line ranges: `deepElementFromPoint` L238–251 + `isElementFixed` L252–264 → module-private hit-test helpers (kept verbatim); `isActive` L342 → `#active` `$state` (read via `isActive` getter); `HoverInfo` type L134–140 → exported `HoverInfo`; `hoverInfo`/`hoverPosition` L372–373 → `$state`; mousemove effect L1838–1876 → `#handleMouseMove`; click effect L1926–2079 → `#handleClick` (single-click create path only); custom-cursor effect L1793–1829 → `#injectCursor`/`#removeCursor` (`feedback-cursor-styles`; crosshair + text-element `text` cursor, `[data-agentation-root]` exclusion); `deactivate` L1628–1641 + reset-on-deactivate effect L1769–1785 → `deactivate()`; unmount safety L1786–1792 → `destroy()`. **Lifecycle is imperative** (`activate`/`deactivate`/`destroy` attach/detach listeners + cursor) rather than `$effect`-driven — same precedent as the annotations controller (deterministic, testable outside an effect-root); upstream keys the same "listeners live only while active" contract on `useEffect` cleanup. **Decoupling:** the picker never touches sibling state — it reads pending/editing through injectable `isPending`/`isEditing` getters (gate hover + click, mirroring upstream L1840/L1995/L2003) and reports built pending data through an `onPick` callback the toolbar wires to the annotations controller's `pending`. **Deliberately dropped (out of scope), each marked `// DIVERGENCE(upstream):`:** `identifyElementWithReact` → plain `identifyElement` (`name === elementName`, `reactComponents` undefined — Phase 7); `detectSourceFile` → `sourceFile` undefined (`utils/source-location.ts` unported, Phase 7); `justFinishedDragRef` + cmd+shift multi-select L1932–1980 (Phase 3); `settings.blockInteractions` interactive-element branch L1982–1993 (settings controller); pending/editing popup `.shake()` L1995–2009 (popup component — the picker just declines to create a second pending); draw/design click branches (Phases 3/6). New vitest+jsdom tests (20) cover activate→hover→click→pending data (incl. the `isFixed` viewport-vs-document coordinate branch), the toolbar/popup/marker hover+click guards, open-shadow-root piercing (stubbed `elementFromPoint` since jsdom has no layout), pending/editing gating, and listener+cursor cleanup on deactivate/destroy. **`internal/annotations.svelte.ts`** done (#16) — the annotation-list state machine. Maps these monolith line ranges: `annotations` state L343 → `#annotations` `$state`; `pendingAnnotation` shape L374–403 → `PendingAnnotation` type + `pending` `$state`; `editingAnnotation` L418 → `editing` `$state`; `isRenderableAnnotation` L265–268 → ported helper; mount-and-load effect L680–685 → `load()` (theme/position loading at L695–717 is other controllers); save effect L1221–1234 → `#persist()` (called imperatively per mutation, not a `$effect` — deterministic + testable outside an effect-root); `addAnnotation` L2590–2641 → `add()`; `cancelAnnotation` L2689–2696 → `cancelPending()`; `deleteAnnotation` L2698–2736 → `remove()`; `startEditAnnotation` L1877–1879 → `startEdit()`; `updateAnnotation` L2815–2829 → `update()` (comment-only, L2820); `cancelEditAnnotation` L2856–2864 → `cancelEdit()`; `clearAll` L2867–2934 → `clearAll()`; `visibleAnnotations` filter L3510–3513 → `visibleAnnotations` `$derived`. **Deliberately dropped (out of scope):** every `originalSetTimeout`/exit-animation, `exitingMarkers`/`deletingMarkerId`/`renumberFrom`/`animatedMarkers` (markers controller, p2-03); server sync (`syncAnnotation`/`deleteAnnotationFromServer`/`updateAnnotationOnServer`) + webhooks + the endpoint/session protocol-field stamping in `addAnnotation` L2616–2626 (Phase 4); editing/hover target-element DOM live-tracking L1884–1923 (picker/markers); `clearAll`'s draw-stroke/design/rearrange/wireframe clearing L2870–2930 (other controllers). **Behavioral choice:** `clearAll()` also resets `pending`/`editing` so no popup dangles over the emptied list (upstream leaves them, relying on the marker animation); `#persist()` keeps upstream's sync-marker branch via an injectable `sessionId` getter so Phase 4 can wire sync without touching the controller. `now`/`generateId` are injectable (default `Date.now`) for deterministic tests. New vitest+jsdom tests (24) cover the full add → edit → delete → clear cycle, the localStorage round-trip through a fresh controller, sync-marker stamping, the renderable + kind filters on load and in the derived, and reactive recomputation. **`internal/markers.svelte.ts`** done (#18) — marker presentation: timed show/hide, per-marker animation sets, delete renumbering, staggered clear, scroll tracking, and hover re-resolution. Maps these monolith line ranges: `markersVisible`/`markersExiting` L370–371 + `isClearing`/`hoveredMarkerId`/`hoveredTargetElement(s)`/`deletingMarkerId`/`renumberFrom` L409–417 + `scrollY`/`isScrolling` L426–427 → `$state`; `animatedMarkers`/`exitingMarkers` L609–612 → `SvelteSet`; the unified visibility effect L650–679 (350ms enter settle / 250ms exit unmount) → `setVisible()`; scroll effect L1197–1219 → `start()`/`destroy()` + `#handleScroll` (`scrollY`, 150ms `isScrolling` debounce; passive listener added once, removed on teardown); `deleteAnnotation` timing L2715–2748 → `startDelete()`; `clearAll` timing L2868–2938 (`count*30+200` stagger) → `startClear()`; `handleMarkerHover` L2754–2813 → `handleMarkerHover()`; `deepElementFromPoint` L238–251 → module-private helper (kept verbatim — picker ports the same; upstream defines it once at module scope). **Lifecycle is imperative** (`start`/`destroy` + `setVisible`) not `$effect`-driven — same precedent as the sibling controllers; `setVisible()` reproduces `useEffect([shouldShowMarkers])` semantics by hand (no-op when unchanged; clear the prior branch's timer before the next). **Decoupling:** keeps marker *timing* here and list *mutation* in the annotations controller — reads the list via injectable `getAnnotations`, asks for removal/clear via `removeAnnotation`/`clearAnnotations` the toolbar wires to `annotations.remove`/`clearAll`. **Timers** use `originalSetTimeout` (so a page freeze never freezes marker animations) and are injectable (`scheduleTimeout`/`cancelTimeout`) for fake-timer tests — required because `originalSetTimeout` is captured at module load and `vi.useFakeTimers()` can't reach it. **Deliberately dropped (out of scope), each marked `// DIVERGENCE(upstream):`:** the `!isDesignMode` term of `shouldShowMarkers` (Phase 6); the `cleared` "Cleared!" button state L2908/L2940 (toolbar button state); the edit-popup exit animation on delete `editExiting` L2705–2713 (popup; `annotations.remove()` already closes the editor); draw-stroke/design/rearrange/wireframe clearing L2870–2930 + server sync/webhooks throughout (other controllers / Phase 4/6); the marker DOM/SVG render layers L4183–4265 (p2-07). **Behavioral choices:** `SvelteSet` mutated in place rather than upstream's `new Set(prev)` reference-identity rebuild (Svelte tracks collection ops; lint `svelte/prefer-svelte-reactivity` enforces it); the enter-settle reads the live list at fire time vs upstream's closed-over snapshot; because the annotations controller unifies callback + list mutation, `onAnnotationDelete`/`onAnnotationsClear` fire when the list actually mutates (end of the exit/stagger) rather than immediately. New vitest+jsdom tests (20) cover show/hide timing (incl. the no-op-when-unchanged dep semantics and the toggle-off-before-settle timer clear), delete renumbering (and the no-renumber-on-last case), the staggered clear, scroll debounce + listener teardown, and hover re-resolution (single/multi-select, the scrollY offset, fixed vs scrolling, and the size-ratio child rejection). **`components/page-toolbar/`** done (#24) — the toolbar shell that *composes* the four controllers + child components. **Portal split:** React `createPortal(jsx, document.body)` (monolith L3565/L4705) has no Svelte primitive, so the dir splits into **`index.svelte`** (the public component a host renders anywhere; on `onMount` it `mount()`s the UI into `document.body` and `unmount()`s it on teardown — the issue's mandated `mount()` substitute) and **`Toolbar.svelte`** (the real shell + every rune). Keeping all reactive state inside `Toolbar.svelte` is deliberate: `mount()` props aren't reactive across the boundary, so nothing shell-scoped is threaded as a plain prop. **Composition:** constructs `AnnotationsController`/`MarkersController`/`PickerController`/`SettingsController` and wires the contracts the controllers were built for — `picker.onPick → annotations.pending`, `picker.isPending/isEditing → annotations.pending/editing !== null`, marker click → `markers.startDelete`/`annotations.startEdit`, popup submit/cancel/delete → `annotations.add`/`update`/`cancelPending`/`cancelEdit` + `markers.startDelete`, clear → `markers.startClear` (→ `annotations.clearAll`), and the marker layers/overlay read the markers controller's animation/scroll/hover state. **Ported in scope:** the body-level event-propagation blocker (L348–369, `mousedown`/`click`/`pointerdown` on `document.body`, `stopPropagation` when the target is inside the `display:contents` wrapper); the collapsed-pill⇄expanded morph with the `IconListSparkle` + count badge (L3604–3616) and the control row (pause/eye/copy/clear/settings/exit) with `.buttonTooltip` + `.shortcut` hints (L3618–3849); toolbar dragging (L3216–3316) with the 10px threshold, viewport `constrain()` (shared by the resize effect L3318–3363), persisted `feedback-toolbar-position` (L740–753), and post-drag click suppression (`justFinishedToolbarDrag`); the scrolling + fixed marker layers with exiting ghosts (L4183–4265); the hover overlay — hover highlight, single-select marker-hover outline, hover tooltip (L4279–4438); the pending + edit popups with `PendingMarker` and the clamp/flip `popupStyle` (L4500–4690); the settings-panel toggle with its open/close animation effect (L636–648) and `toolbarNearBottom` placement; and `hide-until-restart` (`loadToolbarHidden`/`saveToolbarHidden`). The `getTooltipPosition` helper (L3519–3563) is ported returning a CSS **string** (matching `AnnotationMarker.tooltipStyle`). **Picker extension:** `PickerController` gained an optional **`onBlocked`** callback fired when a click is declined because a popup is open (additive, non-breaking) — the toolbar wires it to `pendingPopup.shake()`/`editPopup.shake()` (captured via `bind:this`), porting upstream's shake-the-open-popup-on-outside-click (L1995–2009) while keeping the picker popup-agnostic. **Deliberately dropped (out of scope), each `// DIVERGENCE(upstream):` at the site:** copy/send/freeze end-to-end + the keyboard-shortcut handler (p2-12 — pause/copy render with minimal handlers, send omitted as it is webhook/Phase-4-gated); design/layout/draw mode + multi-select overlays + server/demo paths (Phases 3/4/6); the toolbar shell's full `styles.module.scss` parity is now carried (#25, p2-10) — see the dedicated `styles.module.scss` row below; the `pendingExiting`/`editExiting` outline-exit flags stay dropped (the popup self-animates its own exit before `onCancel` nulls state); the public prop surface + `index.ts` exports (p2-11). New vitest+jsdom test `toolbar.test.ts` (8) — see the test row below. |
| `components/page-toolbar-css/styles.module.scss` (2.2k LOC) | `components/page-toolbar/Toolbar.svelte` `<style>` | rewritten | done (#25) | The toolbar shell's stylesheet. p2-09 (#24) carried only the operable subset; this completes shell parity. **Carried:** the host-CSS-leak guards (`:where(button…)` reset + the `:global(svg[fill="none"])` protection), the `:where(.toolbar)` zero-specificity positional defaults (upstream L196–201, issue #146), the shell morph + **entrance/hide animations** (`toolbarEnter`/`toolbarHide` keyframes), control buttons + CSS tooltips incl. the corner-aligned (`buttonWrapperAlignLeft/Right`) and `tooltipBelow` variants, the badge + its **entrance** (`badgeEnter`), marker layers, the overlay outlines/tooltip (incl. `will-change`/`contain` perf hints), the **full light-mode override cascade** (L2137–2223), and the **theme-toggle flash guard** (`disableTransitions`). **Wiring added here (the matching behavior that makes those keyframes reachable, so Svelte's unused-CSS check stays clean):** `showEntranceAnimation` (module-scoped once-per-page-load flag, upstream L688–693), `isToolbarHiding` (the `hideToolbarTemporarily` 400ms exit, L1131–1141), and `handleToggleTheme` (adds `disableTransitions` to the portal wrapper for one rAF, L568–573). **Common divergences (each `// DIVERGENCE(upstream):` at the site):** (a) SCSS `&` nesting flattened to plain CSS (no preprocessor); the per-component Svelte hash replaces the CSS-module hash; class names kept verbatim. (b) The `:where(button…)` reset is trimmed to `:where(button)` — `.controlsContent` only ever contains buttons here (settings inputs live in the separately-scoped child panel) — but the full upstream property list (incl. `font-style`/`letter-spacing`/`text-transform`/`text-decoration`) is restored. (c) The collapsed-pill icon nudge (`.collapsed svg`) and the `disableTransitions` rule use `:global(…)` — the SVG is a child component's and `disableTransitions` is applied imperatively to the wrapper. (d) The light-mode `.controlButton` hover `:not()` chains are trimmed to the wired states (the server `data-error`/`data-auto-sync`/`data-failed`/`data-no-hover` variants are dropped, matching the dark cascade); `.singleSelectOutline` uses `--agentation-color-blue` (upstream L815/822 — corrects a p2-09 slip that used `accent`). **NOT carried (ship with their features, not here):** the settings-panel / marker / popup rules (own components, p2-06/07/08, only the shared `--agentation-*` variables they depend on are reconciled); and the Phase 3/4/6 rules — server/MCP indicators, connection dot, send button, the `serverConnected` width, the extra control-button server states, draw canvas, drag-selection, design/layout styles (their selectors would be unused under Svelte's check). New test: `toolbar.test.ts` gains a `data-feedback-toolbar`-on-every-layer assertion (the freeze-animations exclusion contract). |
| `components/annotation-popup-css/` | `components/annotation-popup/` (`index.svelte`) | rewritten | done (#21) | The comment popup (textarea card: submit/cancel/delete, trap-bypassing focus, invalid-submit shake). `index.tsx` → `index.svelte`; `styles.module.scss` (437 LOC) → the component's scoped `<style>` (class names verbatim, SCSS `&` nesting flattened to plain CSS, the `:where`-free `.popup svg[fill="none"]` host-CSS guard from upstream issue #58, and the shake/popupEnter/popupExit keyframes + grid-rows accordion all preserved). **`shake()` handle:** `forwardRef`/`useImperativeHandle` → an **exported instance function** `export function shake()` (the parent captures it via `bind:this` and calls `popup.shake()`) — the `AnnotationPopupCSSHandle` interface is therefore dropped. **Common divergences (each `// DIVERGENCE(upstream):` at the site):** (a) `useState`→`$state`, `useRef`→`bind:this`/locals (upstream's unread `popupRef` dropped), the two `useEffect`s→`$effect`s (isExiting-sync + the mount enter/focus timers, the latter returning the same teardown that clears the enter/focus/cancel/shake timers); (b) `useCallback` handlers → plain functions; (c) controlled `value`/`onChange`→`bind:value`, the inline `borderColor` focus tint + submit `backgroundColor`/`opacity` → `style:` directives; (d) `className` concat → `class:` directives; (e) `focusBypassingTraps` kept verbatim in a `<script module>` (upstream's module scope); (f) `style?: React.CSSProperties` → `style?: string` (the Svelte `style={...}` idiom; the toolbar passes the position string in p2-09); (g) the `computedStyles` truthiness guard hoisted to a `$derived` (read twice in upstream's JSX). **Behavioral fidelity (resolved against upstream, NOT the issue's AC wording — confirmed with the maintainer):** plain **Enter** (no shift) submits / Shift+Enter newlines (upstream L168 — the AC's "Cmd/Ctrl+Enter" was a slip); empty submit is a **silent no-op** with the submit button `disabled` (upstream L159/L290 — the AC's "empty submit shakes" is not upstream; `shake()` is fired by the *parent* on outside-click, L2000/L2009); root keeps **`data-annotation-popup`** (upstream L192 — already in `freeze-animations`' `EXCLUDE_ATTRS` so the freeze-exclusion contract the AC names holds, and `design-mode/rearrange.tsx` keys on it; the AC's `data-feedback-toolbar` was a slip). **Dropped style rule:** `.textarea.green:focus` — no element ever receives a `green` class in this component (upstream's is dead too; the green focus border comes from the runtime inline `accentColor`), so keeping it would only trip Svelte's unused-CSS-selector warning. **Out of scope (per issue #21):** mounting/positioning inside the toolbar overlay (p2-09); the public `AnnotationPopupCSS` re-export from `index.ts` (p2-11); multi-select/draw placeholder variants (Phase 3). New vitest+jsdom tests (14) cover render/defaults, initialValue seeding, Enter-submit (trimmed) + Shift+Enter-newline, empty/whitespace no-op + disabled button, submit-button submit, Cancel→exit→onCancel (150ms) + Escape, delete-button presence/wiring, the computed-styles kebab accordion, quote 80-char truncation, the 50ms trap-bypassing focus, and `shake()` class-toggle + 250ms clear + restart-on-repeat — the timer tests `vi.mock` `utils/freeze-animations` (so `originalSetTimeout` is fake-timer-interceptable) and dispatch **bubbling** keydown events (Svelte 5 delegates `keydown` to the mount root). |
| `components/page-toolbar-css/annotation-marker/` | `components/page-toolbar/annotation-marker/` (`AnnotationMarker.svelte` + `PendingMarker.svelte` + `ExitingMarker.svelte` + `index.ts` barrel) | rewritten | done (#22) | The three numbered-badge markers. **One file → three files + barrel:** upstream's single `index.tsx` declares all three components; Svelte allows one component per `.svelte` file, so each becomes its own file re-exported under its **identical upstream name** by `index.ts` (same shape as the `icons/` port) — consumers keep `import { AnnotationMarker, PendingMarker, ExitingMarker } from ".../annotation-marker"`. `MarkerClickBehavior` (upstream's module-scope `"edit" \| "delete"`) is exported from `AnnotationMarker.svelte`'s `<script module>` and re-exported by the barrel. **`styles.module.scss` (199 LOC) → per-component scoped `<style>`:** because Svelte hashes classes **and** `@keyframes` per component (a keyframe defined in one component is invisible to another), the SCSS can't be shared across the three files — each carries only the **subset** of classes it applies (`AnnotationMarker`: base + enter/exit/clearing/multiSelect/hovered/renumber + the tooltip + light-theme override; `PendingMarker`: base + pending/multiSelect/enter/exit; `ExitingMarker`: base + exit/fixed/hovered/multiSelect, dropping the inert `:hover`/transition rules since the ghost is `pointer-events: none`). Carrying only used classes also keeps Svelte's unused-CSS-selector check clean. Class names kept verbatim, SCSS `&` nesting flattened to plain CSS (no preprocessor in this repo), keyframe names (`markerIn`/`markerOut`/`tooltipIn`/`renumberRoll`) preserved, and the `[data-agentation-theme="light"]` ancestor selector wrapped in `:global(...)` so the rightmost classes stay scoped (same treatment as the checkbox/switch ports). **Common divergences (each `// DIVERGENCE(upstream):` at the site):** (a) React `className` string-concat → `class:` directives — upstream's `animClass` (exit ▸ clearing ▸ enter priority) becomes `class:enter`/`class:exit`/`class:clearing` driven by `isEnter`/`isExiting`/`isClearingAnim` `$derived`s; (b) the `style` object → `style:` directives — React appends `px` to a numeric `top`, so `top: {y}px` is written explicitly, and `style:background-color={showDeleteHover ? undefined : markerColor}` reproduces upstream's `undefined` (lets the `.hovered` red rule win); (c) `tooltipStyle?: React.CSSProperties` → `string` (the Svelte `style={...}` idiom — the toolbar passes the computed position string in p2-09 — matching the popup port); (d) the inline `selectedText` quote hoisted to a `quoteSuffix` `$derived`; (e) `onContextMenu` is always wired but no-ops unless the prop is set **and** behavior is `delete` (same observable contract as upstream's `undefined`-or-handler). **`data-annotation-marker` kept verbatim (NOT `data-feedback-toolbar`):** it is the attribute upstream uses on the marker, it is already in `freeze-animations`' `EXCLUDE_ATTRS` (so the freeze-exclusion contract the AC names holds), and the marker **layer** the toolbar mounts these into (monolith L4184/L4225, p2-09) carries `data-feedback-toolbar` itself — the AC's "markers carry `data-feedback-toolbar`" was a slip (same pattern as #21's popup, where the AC named `data-feedback-toolbar` but upstream uses `data-annotation-popup`). **A11y:** the marker is a plain clickable `<div>` exactly as upstream (no added `role`/`tabindex`); `<!-- svelte-ignore a11y_click_events_have_key_events, a11y_no_static_element_interactions -->` documents that keyboard interaction is not part of upstream's marker contract (the toolbar list provides the accessible path). **Out of scope (per issue #22):** layer placement / scroll math / which markers render where (p2-03 state, p2-09 composition); the marker hover *tooltip positioning* helper (monolith L3519–3564, p2-09) — the tooltip markup is ported but its `tooltipStyle` is supplied by the toolbar. New vitest+jsdom tests (25) — see the test row below. |
| `components/page-toolbar-css/settings-panel/` | `components/page-toolbar/settings-panel/` (`index.svelte` + `checkbox-field/index.svelte`) | rewritten | done (#23) | The settings panel + its `checkbox-field/` subdir. **Presentational:** the panel owns no state — it takes upstream's exact prop surface (`settings`, `onSettingsChange(patch)`, `isDarkMode`/`onToggleTheme`, `isDevMode`, `connectionStatus`/`endpoint`, `isVisible`, `toolbarNearBottom`, `settingsPage`/`onSettingsPageChange`, `onHideToolbar`) and the toolbar wires those to the settings controller (p2-09). `index.tsx` → `index.svelte` (default export replaces named `SettingsPanel`); no barrel (single-component dir, same as the popup port — the toolbar default-imports `index.svelte`). **The settings model lives in `internal/settings.svelte.ts`, not here** — see the monolith row: upstream's panel imports `{ COLOR_OPTIONS, ToolbarSettings } from ".."` (the monolith), so our panel imports them from the controller module that decomposes that monolith state. **`styles.module.scss` (809 LOC) → scoped `<style>`:** class names verbatim, Svelte hashing replacing the CSS-module hashing, SCSS `&` nesting flattened to plain CSS, `@keyframes` (`cycleTextIn`/`mcpPulse`/`mcpPulseError`/`themeIconIn`) preserved. `[data-agentation-theme="…"]` ancestor selectors (theme attr lives on the toolbar root, outside scope) wrap the ancestor in `:global(...)` so the rightmost class stays scoped — same treatment as the sibling ports. **Dropped dead SCSS rules** (defined upstream but never applied by the panel JSX, so Svelte would flag them unused): `.settingsBrandSlash`, `.settingsOption`, `.dropdownButton`, `.slider`, `.sliderLabel`. **Cross-scope rules → `:global` (same pattern as help-tooltip):** `.checkboxField:not(:first-child)` margin targets the *second* `CheckboxField`, whose `class` lands on a child-component root, so it is a scoped-ancestor + `:global(.agentation-settings-checkbox-field…)` descendant; the back-button chevron is the `IconChevronLeft` *component* (its `<svg>` is in its own scope), so `.settingsBackButton svg` → `.settingsBackButton :global(svg)`. **Common divergences (each `// DIVERGENCE(upstream):` at the site):** (a) `className` concat → `class:` directives; the `style={toolbarNearBottom ? {…} : undefined}` placement override → `style:bottom`/`style:top`; (b) React's `key={…}` remount-to-replay-animation on the theme icon + output-detail label → `{#key …}` blocks; (c) the color swatch `--swatch`/`--swatch-p3` custom props → `style:` directives; (d) `e.target` → `e.currentTarget` in the `Switch`/`CheckboxField` change handlers (Svelte sets `currentTarget` to the element inside the delegated handler), and the controlled webhook `<textarea onChange>` → `oninput` (same live-update semantics). **Additive attributes (documented at the site):** the panel root keeps upstream's `data-agentation-settings-panel` **and** adds `data-feedback-toolbar` — upstream tags peer toolbar-UI subtrees (the tooltip component L74, the design-mode panels) with it so the picker's `closest("[data-feedback-toolbar]")` hit-test ignores them and freeze-animations excludes them; carrying it makes the panel picker-/freeze-safe even before the toolbar wraps it (p2-09). The brand `<a>` (logo-SVG-only) gains an `aria-label` (Svelte a11y; upstream has none). **`checkbox-field/`:** `index.tsx` → `index.svelte`; React `useId()` → `$props.id()` (ties `<label for>`/`<input id>`); `CheckboxFieldProps extends React.HTMLAttributes<HTMLDivElement>` → `Omit<HTMLAttributes<HTMLDivElement>, 'onchange'>` + the input `onchange` (avoids a structural clash); `...props` spread onto the container preserved; its 18-LOC SCSS → scoped `<style>` (`[data-agentation-theme="dark"] &` → `:global`). **`v{__VERSION__}` → `v{VERSION}`** from `internal/version.ts` — svelte-package doesn't run Vite's `define`, so a bare `__VERSION__` would ship un-replaced and throw `ReferenceError`; `version.ts` reads it through a `typeof` guard (real value in dev/test/playground via the new `vite.config.ts` `define`, empty string in the packaged build; revisited at publish, #49). **Out of scope (per issue #23):** live `connectionStatus`/endpoint behavior (Phase 4 — the disconnected state renders, props exist for compat); React-detection toggle *behavior* (`reactEnabled` persists but drives nothing until Phase 7); webhook *sending* (Phase 4 — fields render + patch state only); mounting the panel + open/close animation state in the toolbar (p2-09). New vitest+jsdom tests (26) — see the test rows below. |
| `components/checkbox/`, `switch/`, `tooltip/`, `help-tooltip/` | same paths (each `index.svelte`) | rewritten | done (#20) | The four presentational primitives. Each upstream dir's `index.tsx` becomes `index.svelte`; SCSS modules become the component's scoped `<style>` (class names preserved verbatim, Svelte hashing replacing the CSS-module hashing). **Common divergences (each `// DIVERGENCE(upstream):` at the site):** (a) React named export → Svelte default export (consumers default-import, e.g. help-tooltip's `import Tooltip from "../tooltip/index.svelte"`); (b) `className` prop + `...props` → Svelte `class` prop (renamed at destructure, reserved word) + `{...rest}`; (c) SCSS `&` nesting flattened to plain CSS (no preprocessor in this repo — there is no `svelte.config.js`); (d) `[data-agentation-theme="…"] &` ancestor theme selectors wrap the ancestor in `:global(...)` (the theme attribute lives on the toolbar root, outside each component's scope) so the rightmost class stays scoped and nothing leaks. **`checkbox`** + **`switch`**: styled native `<input type=checkbox>` passthrough — `class`→container `div`, `rest` (checked/disabled/onchange/…)→input; the `:has(.…Input:checked)` container reaction is preserved (flattened). **`tooltip`**: React `useState`→`$state`, `useRef`→`bind:this`/locals, the unmount-cleanup `useEffect`→`$effect` teardown clearing both delay timers; **`createPortal(…, document.body)` → an `{@attach}` attachment** that `appendChild`s the tooltip node to `<body>` and removes it on teardown (node stays Svelte-managed so `position`/`visible` reactivity keeps patching it); all inline styles kept inline (upstream has no SCSS for tooltip), with dynamic `top`/`right`/`opacity` interpolated; **keeps `originalSetTimeout`** for the 500ms show / 150ms exit timers and `data-feedback-toolbar` on the portaled node (freeze-animations exclusion contract). **`help-tooltip`**: composes `Tooltip`+`IconHelp`; because the `.tooltip`/`.tooltipIcon` classes land on **child-component** roots (`<Tooltip>`'s span, `<IconHelp>`'s svg) Svelte can't add this component's scope hash to them — so the rules are `:global` with **namespaced** names (`tooltip`→`agentation-help-tooltip`, `tooltipIcon`→`agentation-help-tooltip-icon`) restoring the collision-proofness upstream's CSS-module names already had (the toolbar mounts into the host `<body>`, no shadow DOM, so a bare global `.tooltip` would leak onto the host page). New vitest+jsdom tests (per dir): checkbox/switch (5 each — structure, checked/unchecked, `class`→container, rest-props→input), tooltip (4 — no-tooltip-until-hover, rest-props→span, the full 500ms-show→150ms-exit cycle incl. opacity + `data-feedback-toolbar`, and show-cancel-on-early-leave), help-tooltip (2 — composition + content-forwarding-on-hover). The timer tests **mock `utils/freeze-animations`** so `originalSetTimeout` routes through the global `setTimeout` that `vi.useFakeTimers()` can intercept (it's captured native at module load otherwise), and `flushSync()` after each interaction forces Svelte's batched DOM update. Shared `mount`/auto-cleanup helper extracted to `src/test/svelte-render.ts`. |
| `components/icons.tsx` (1073 LOC) | `components/icons/*.svelte` + `components/icons/index.ts` | rewritten | done (#19) | Upstream's single `.tsx` (36 pure-SVG function components) becomes **one `.svelte` file per icon** under `components/icons/`, re-exported under its **identical upstream name** by `components/icons/index.ts` (`export { default as IconClose } from './IconClose.svelte'`, upstream declaration order) — so `components/index.ts` keeps `export * from "./icons"`. SVG markup is near-verbatim; the mechanical work is JSX→HTML attribute casing (`strokeWidth`→`stroke-width`, `clipPath="…"`→`clip-path="…"`, `className`→`class`, `textAnchor`/`fontSize`/… on `AnimatedBunny`'s `<text>`). **`size` defaults preserved** verbatim. **Divergences (each `// DIVERGENCE(upstream):` at the site):** (1) **React object/spread props → Svelte rest spread:** `IconHelp`'s `...props` (`React.SVGProps`) and `IconListSparkle`/`IconChatEllipsis`'s `style` (`React.CSSProperties`) become a single `{...rest}: SVGAttributes<SVGSVGElement>` spread onto the root `<svg>` (callers pass a CSS *string* via `style`, or any SVG attr, through the same spread; upstream's default `style={}` is a dropped no-op). `IconCopyAnimated`'s `tint` → an inline `style` string (`color: …; transition: color 0.3s ease`). (2) **CSS module → scoped `<style>`:** the 5 two-state animated icons (`IconCopyAnimated`, `IconSendArrow`, `IconSendAnimated`, `IconEyeAnimated`, `IconPausePlayAnimated`) imported `icon-transitions.module.scss` as `s`; each now carries the subset of those classes it uses in its own scoped `<style>` (Svelte's per-component hashing replaces the module's hashing — no global class, immune to host collisions), with `!important` **preserved** so a host `svg * { opacity: 1 !important }` can't force both states visible (upstream issue #58). Upstream's `` `${s.iconState} ${cond ? s.hiddenScaled : s.visibleScaled}` `` string concatenation becomes idiomatic `class="iconState" class:hiddenScaled={…} class:visibleScaled={…}` directives (compiler sees the class names → no unused-CSS-selector warning). `IconSendArrow`'s `state` prop drives `$derived` `showArrow`/`showCheck`/`showError`/`isSending`. (3) **Inline `<svg><style>` → top-level scoped `<style>`:** `IconCheckSmallAnimated` and `AnimatedBunny` embedded a `<style>` *inside* the `<svg>` (Svelte forbids that — one top-level `<style>` only); keyframes + classes moved to the component's top-level `<style>` verbatim, applied via static `class="…"` so they aren't pruned. Animated icons keep the same state-prop names (`isPaused`, `isOpen`, `copied`, `sent`, `state`, `tint`, `color`). New vitest+jsdom smoke tests (45) mount **every** icon and assert `<svg>` + the upstream `viewBox` + default-size width/height (manifest cross-checked against the barrel's exports so a missing/extra/renamed icon fails), custom-`size` forwarding, rest-prop pass-through (`IconHelp` `aria-label`), and the two-state class toggles for all 5 animated transition icons + `AnimatedBunny`'s `color`. **Tooling:** first `.svelte`-component render under vitest forced adding `resolve.conditions: ['browser']` (gated on `VITEST`) to `vite.config.ts` so `import { mount } from 'svelte'` resolves the client build, not the server build where `mount()` throws (per Svelte testing docs). |
| `components/icon-transitions.module.scss` (51 LOC) | (inlined into the 5 animated icon `.svelte` `<style>` blocks) | rewritten | done (#19) | Not a standalone file — the CSS-module classes (`iconState`/`iconStateFast`/`iconFade`/`iconFadeFast` transitions + `visible`/`hidden`/`visibleScaled`/`hiddenScaled`/`sending` states) are ported into each consuming animated icon's scoped `<style>`, only the subset each uses. Svelte component scoping supplies the hashing the `.module.scss` provided; the `!important` on the opacity rules is preserved verbatim (the host-`!important`-override contract from upstream issue #58, restated in a comment at each site). |
| `components/design-mode/` (~4k LOC) | — | deferred | Phase 6 | spatial/section/output logic largely portable; UI rewritten |

## Tests

| Upstream | Ours | Mode | Status | Divergences |
|---|---|---|---|---|
| (none upstream) | `utils/element-identification.test.ts` | n/a (new) | done (#7) | upstream ships no test for this module; new jsdom unit tests cover identify/path/shadow/nearby/classes/a11y |
| (none upstream) | `utils/freeze-animations.test.ts` | n/a (new) | done (#9) | upstream ships no test; new jsdom unit tests cover timer/RAF queueing while frozen, `original*` bypass, unfreeze flush + style removal, and the three `data-*` exclusion attrs in the injected CSS |
| (none upstream) | `utils/screenshot.test.ts` | n/a (new) | done (#10) | upstream ships no test; new jsdom unit tests cover `isDomCaptureAvailable` once-only probe caching (positive + negative, asserted via a throwing `vi.mock` factory's call count), `captureDomRegion` null path when the dep is absent, and the `captureDrawingStrokes` fallback against a stubbed 2D context (render, `<2`-point skip, null-context, serialization-throw) — control flow only, no pixel assertions |
| `utils/react-detection.test.ts` | — | skipped | n/a | tests a skipped file |
| `utils/source-location.test.ts` | — | skipped | n/a | tests a skipped file |
| `components/page-toolbar-css/index.test.tsx` | rewritten per component | rewritten | in progress | per-component suites done (popup #21, markers #22, settings #23, toolbar #24); copy/clear/keyboard/freeze/design behavior tests follow in p2-12 / later phases |
| (none upstream) | `components/page-toolbar/toolbar.test.ts` | n/a (new) | done (#24) | upstream's monolith test is rewritten per component; this covers the toolbar shell composition. New vitest+jsdom tests (8): the UI mounts into `document.body` (not the host target) with `data-feedback-toolbar`; the portal wrapper carries `data-agentation-theme="dark"`/`-accent="blue"`; clean unmount leaves no `.toolbar`/`[data-agentation-root]` nodes; collapsed→click→expanded morph (`.collapsed`→`.expanded`, `.controlsContent.visible`, the `P` shortcut hint); the count badge from seeded storage (3 → "3", resolved/dismissed excluded → "1", none → no badge); and the event-propagation blocker (a click inside the toolbar never reaches a `document` listener, while a click elsewhere does). **#25 adds a 9th test:** `data-feedback-toolbar` stays on every toolbar-owned layer (shell + both marker layers always, the overlay once active) — the freeze-animations exclusion contract that style refactors must preserve. Mounts `index.svelte` directly (not the shared `render()` helper) and asserts against `document.body`, since the defining behavior is the body portal; seeds via `utils/storage` so the annotations controller hydrates as in the browser; `flushSync()` after mount runs `onMount` so the UI lands in the body. |
| (none upstream) | `components/icons/icons.test.ts` | n/a (new) | done (#19) | upstream ships no test for `icons.tsx`; new vitest+jsdom smoke tests (45) mount every icon via Svelte's `mount()` and assert the `<svg>` + upstream `viewBox` + default-size width/height (the expected manifest is cross-checked against the barrel's own exports, so a missing/renamed/extra icon fails the suite), plus custom-`size` forwarding, `IconHelp` rest-prop pass-through, and the two-state class toggles for the 5 animated transition icons + `AnimatedBunny`'s `color`. First component render under vitest is what required the `resolve.conditions: ['browser']` addition to `vite.config.ts` (see the `icons.tsx` row). |
| (none upstream) | `components/annotation-popup/annotation-popup.test.ts` | n/a (new) | done (#21) | upstream ships no test for the popup; new vitest+jsdom render tests (14) — see the `annotation-popup-css/` component row above for the full list. `vi.mock`s `utils/freeze-animations` (fake-timer-interceptable `originalSetTimeout`) and dispatches **bubbling** keydown events (Svelte 5 delegates `keydown` to the mount root, so non-bubbling synthetic events never reach the handler). The focus/shake timing tests `flushSync()` after `mount` so the mount `$effect` schedules its timers before `vi.advanceTimersByTime`. Reuses the shared `src/test/svelte-render.ts` `mount`/auto-`unmount` helper; the `shake()` test mounts directly to capture the instance handle. |
| (none upstream) | `components/page-toolbar/annotation-marker/annotation-marker.test.ts` | n/a (new) | done (#22) | upstream ships no test for the markers; new vitest+jsdom render tests (25). **AnnotationMarker (19):** badge number (`globalIndex + 1`), `x%`/`ypx` positioning + accent color, green multi-select color + `.multiSelect` class, hover number→edit-icon swap (`edit` behavior, distinguished by IconEdit's `0 0 16 16` viewBox vs IconXmark's `0 0 24 24`), hover number→delete-icon swap + `.hovered` (`delete` behavior), `isEditingAny` suppressing the swap, `isDeleting` forcing the delete icon without hover, the `renumber` class only at/after `renumberFrom`, the staggered `animationDelay` (`layerIndex*20` on enter, `(layerSize-1-layerIndex)*20` on exit), `onHoverEnter`/`onHoverLeave` on mouse enter/leave, `onClick` firing with the annotation + `stopPropagation` (asserted via a `document.body` ancestor listener, since Svelte delegates `click` to the mount root so a listener *on* the root fires regardless), the no-click-while-exiting guard, `onContextMenu` only firing under `delete`, the hover tooltip (element + comment) with 30-char `selectedText` truncation, and tooltip omission while editing. **PendingMarker (3):** plus-icon + `.marker.pending` + positioning, enter/exit class toggle, green multi-select. **ExitingMarker (3):** xmark ghost with `.marker.hovered.exit` + positioning, `.fixed` class, `.multiSelect` class. Reuses the shared `src/test/svelte-render.ts` helper via a local `renderMarker` that spreads typed `AnnotationMarkerProps` into a fresh object literal (so it satisfies `render`'s `Record<string, unknown>` param — same idiom as the popup test); `click`/`contextmenu` dispatched with `{ bubbles: true }` (delegated events), `flushSync()` after each. No timers, so no `freeze-animations` mock needed (the markers animate purely via CSS). |
| (none upstream) | `components/page-toolbar/settings-panel/settings-panel.test.ts` + `checkbox-field/checkbox-field.test.ts` | n/a (new) | done (#23) | upstream ships no tests for the panel. **`settings-panel.test.ts` (20):** the enter/exit visibility class, the `data-agentation-settings-panel` + `data-feedback-toolbar` root attrs, the rendered version, the `toolbarNearBottom` placement override; each control's patch (output-detail cycle `standard`→`detailed`, accent color, auto-clear + block-interactions checkboxes, the React switch in dev mode + its disabled state out of dev mode, the hide-until-restart switch → `onHideToolbar`, the theme toggle); main↔automations page switching + the `slideLeft`/`slideIn` classes; the webhook auto-send switch (enabled only with a URL) + the textarea `oninput` patch; and the render-only disconnected status dot (no nav indicator) vs the connected nav indicator. **`checkbox-field.test.ts` (6):** checkbox + label render, the `for`/`id` association via `$props.id()`, the `class`→container, the `checked` reflection, the `onchange` forward (asserted via `e.target` since the delegated handler resets `currentTarget` after it returns), and conditional tooltip rendering. Both reuse `src/test/svelte-render.ts`; `change`/`input` dispatched with `{ bubbles: true }` (Svelte delegates them) + `flushSync()` after each. |
| (none upstream) | `components/{checkbox,switch,tooltip,help-tooltip}/*.test.ts` + `src/test/svelte-render.ts` | n/a (new) | done (#20) | upstream ships no tests for these primitives; new vitest+jsdom render tests per dir (checkbox 5, switch 5, tooltip 4, help-tooltip 2) — see the primitives row above for what each covers. Tooltip/help-tooltip tests `vi.mock` `utils/freeze-animations` so `originalSetTimeout` becomes fake-timer-interceptable and `flushSync()` after each interaction. `src/test/svelte-render.ts` is a shared `mount()`/auto-`unmount` helper (registers its own `afterEach`), factored out of the inline helper in `icons.test.ts`. |
| (new) compat fixtures | `tests/fixtures/` + `tests/compat.test.ts` | n/a (new) | done (#14) | Two-part parity harness for `generate-output.ts`. `scripts/generate-fixtures.ts` (`pnpm fixtures:generate`) feeds `tests/fixtures/annotations.json` (empty / minimal / maximal cases) to **upstream's own** `utils/generate-output.ts` — imported via `tsx` (new devDep) from the pinned `upstream/` clone; esbuild elides upstream's type-only enum import from the React toolbar, so it runs in Node — and writes `tests/fixtures/output/<case>.<level>.md` for all 4 levels. `tests/compat.test.ts` (`@vitest-environment node`) feeds the identical inputs to **our** `generateOutput` and asserts strict string equality. Both run window-absent so output is deterministic (viewport `unknown`, no URL/UA/Timestamp/DPR); regeneration is idempotent (zero git diff on re-run). Each fixture carries a generated-file header + never-hand-edit rule (shared envelope in `tests/fixtures-format.ts`, stripped before comparison); rule restated in the generator and `tests/fixtures/README.md`. Fixtures + script are Prettier-ignored via the existing `*.md`/`*.json`/`scripts/` globs. The maximal case exercises `selectedText` under/over the 30-char compact cutoff, `nearbyText` with/without `selectedText` (suppression rule), the `isMultiSelect && fullPath` first-element note (one annotation hits it, one omits it), `boundingBox`, `cssClasses`, `computedStyles`, `accessibility`, `nearbyElements`, and the React-only `sourceFile`/`reactComponents`. |
