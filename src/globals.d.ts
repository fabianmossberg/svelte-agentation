// Build-time constants injected by Vite `define` (vite.config.ts).
//
// Mirrors upstream's `src/globals.d.ts`. See `src/lib/internal/version.ts` for
// why the value is read through a `typeof` guard rather than relied on directly.
declare const __VERSION__: string;
