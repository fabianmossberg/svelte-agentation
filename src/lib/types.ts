// Stub — the Annotation schema and output enums are ported in Phase 1.
// Do not add upstream content here: that port is owned by
// issue #6 "Port types.ts, absorbing OutputDetailLevel and ReactComponentMode".
// This file exists now only so the src/lib tree mirrors upstream package/src
// and the Phase 1 PR lands as a clean stub -> ported-file diff.

export {};
