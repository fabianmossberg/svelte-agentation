// Package entry — mirrors upstream package/src/index.ts.
//
// This stub exports nothing yet. The real export list (Agentation, the popup,
// icons, utils, and the Annotation type) is assembled in Phase 2 once the
// ported modules exist; matching upstream's export surface is owned by that
// phase, not this scaffolding issue.

export {};
